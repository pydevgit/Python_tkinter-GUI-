# import all class inside of tkinter library
from tkinter import *

root = Tk()
# here define a label nothing but (text)
myLabel = Label(root, text="I,m root window ")
myLabel.pack
# it hold the window
root.mainloop()
